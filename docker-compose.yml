version: '3'
services:
  nginx:
    build: ./nginx
    ports:
      - '80:80'
      - '8443:8443'
  mongo:
    image: mongo:4.2.5
    ports:
      - '27017:27017'
  app:
    build: .
    environment:
      WAIT_HOSTS: mongo:27017
    ports:
      - '8080:8000'
